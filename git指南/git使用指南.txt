1、git config --global user.name 用户名
2、git config --global user.email 用户邮箱
3、克隆gitHub上的项目
	git clone url(http)
4、新建git项目
	git init
5、 将代码再git上备份
	git add .(.是当前文件夹)
	git commit -m "(备注信息)"
6、查看提交的历史记录
	git log
7、恢复文件到某个历史版本，进行覆盖
	git checkout HEAD 文件名
8、提交某个特定文件时把 .改成文件名，其余同
